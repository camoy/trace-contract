* `random` has constraint on the number of times called
* `random-between` has constaint likewise
* `new-corridor` and `new-room` must be called in a specific sequence
* `try-add-rectangle`
* `grid-ref`
* `within-grid?`
