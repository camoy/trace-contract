#lang info

;; general

(define name "trace-contract")
(define collection "trace-contract")
(define pkg-desc "")
(define version "0.0")
(define pkg-authors '(camoy))

;; dependencies

(define deps
  '("base"
    "ee-lib"))

(define build-deps '())
